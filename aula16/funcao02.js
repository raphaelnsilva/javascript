function soma(n1=0, n2=0) { // n1 vai valer 2, e n2 vai valer 5. '=0' vai fazer que se  a pessoa não digitar nenhum numero, o n1 ou o n2 vao valer 0  
    
    return n1 + n2 
}
console.log(soma(2, 5)) // 2 e 5 vão para a função lá em cima 